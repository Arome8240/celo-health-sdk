/** Average Celo block time in milliseconds (~5 seconds). */
export const MS_PER_BLOCK = 5_000;

/** Approximate Celo blocks per day. */
export const BLOCKS_PER_DAY = 17_280;

/** Approximate Celo blocks per week. */
export const BLOCKS_PER_WEEK = 120_960;

/** Approximate Celo blocks per month (30 days). */
export const BLOCKS_PER_MONTH = 518_400;

/** Approximate Celo blocks per year (365 days). */
export const BLOCKS_PER_YEAR = 6_307_200;

export const TEST_ING = 12
